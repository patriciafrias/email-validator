version: '3'

services:
  mysql:
    environment:
      - MYSQL_ROOT_PASSWORD=project
      - MYSQL_DATABASE=project
      - MYSQL_USER=project
      - MYSQL_PASSWORD=project
  apache:
    environment:
      - APP_DOMAIN=project.local
      - APP_DOC_ROOT=/var/www/public
      - APACHE_SERVERNAME=server.project.com
